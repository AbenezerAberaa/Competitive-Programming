class Solution:
    def isPalindrome(self, x: int) -> bool:
        concat = ""
        x = str(x)
        for each in range(len(x)-1,-1,-1):
            concat += x[each]
        if x == concat:
            return True
        else:
            return False
